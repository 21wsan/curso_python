# separar cadenas (split)

datos = "hola mundo"
lista = datos.split() # por defecto separa los elementos por espacios en blanco.
print(lista)

# separa la cadena por la (,) colocando los elementos en una lista
datos = "jhon,Doe,30,Colombia"
lista = datos.split(",")
print(lista)